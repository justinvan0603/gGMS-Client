import {Component} from '@angular/core';
import { Router } from '@angular/router';
@Component({
  selector: 'domainusers',
  template: `<router-outlet></router-outlet>`
})
export class DomainUsers {

  constructor() {
  }
}
